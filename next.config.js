/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
    domains: ['tccbnjsgrmpqhdaqzwtv.supabase.co'],
  },
}

export default nextConfig
